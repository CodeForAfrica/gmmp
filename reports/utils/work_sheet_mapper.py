"""
Maps `worksheet` from `2010` to its corresponding `worksheet` on `2015`
so we can track the data and write it to the correct key in `historical.json`.
"""

mapper = {
    '1F': '1',
    '9aF': '5',
}
